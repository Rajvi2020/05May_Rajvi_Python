class->class is a collection of member functions and  member data

object->object is an instance of class

inheritance -> child class inheritances  proprities of parent class

encapusulation -> bindind of data and data members into single unit

polumorphism-> one name multiple forms
  method overloading->same method name but diff arguments
  method overriding->same method name ,same argument name but in diff class

access specifires->how you can access class data members and members functions
  private->can not access outside the class
  public->can access outside the class

  x=re.findall("[]a-z),mystr
  dot all
  re.fndall('py..on),mystr
  same as averbose findall